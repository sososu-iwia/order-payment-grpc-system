# Payment Service — copy to .env and fill in values

# PostgreSQL connection string
PAYMENT_DB_DSN=postgres://postgres:postgres@localhost:5434/payment_db?sslmode=disable

# HTTP REST port (for end-users / curl)
PORT=8082

# gRPC port (for Order Service internal calls)
GRPC_PORT=50051
